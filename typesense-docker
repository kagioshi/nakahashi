FROM typesense/typesense:latest

# Expose the port Choreo will listen to
EXPOSE 8108

# You do NOT set ENV here in Dockerfile for sensitive values
# Choreo lets you define environment variables at deploy time

